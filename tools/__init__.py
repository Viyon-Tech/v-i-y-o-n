"""VIYON tools — all shell/file/network side effects funnel through here; agents never touch subprocess directly."""

# TODO: implement (Phase per CLAUDE.md build order). Scaffold only — no logic yet.
